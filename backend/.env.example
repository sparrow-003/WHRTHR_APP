# Backend Configuration

PORT=3000
NODE_ENV=development

# Security & Authentication
# Generate a strong admin API key for cache clearing operations
# Example: openssl rand -base64 32
ADMIN_API_KEY=your-super-secret-admin-api-key-change-this-in-production

# API Sources (already configured in code)
# OPEN_METEO_API=https://api.open-meteo.com/v1
# GEO_API=https://geocoding-api.open-meteo.com/v1
